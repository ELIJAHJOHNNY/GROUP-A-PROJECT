import { createSlice } from "@reduxjs/toolkit";

const initialState = {

}

const hamburgerSlice = createSlice({
    name: 'hamburger',
    initialState,
    reducers:{

    }
})


export default hamburgerSlice.actions